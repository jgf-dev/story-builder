---
created: 2026-07-15T07:11:21.712Z
session: 77381f08-9c16-47ec-b0fd-33663f8bb1ae
working_directory: /home/jgf2/git/voice/story-builder
---

## Plan: File Linear issues for all findings, then fix the 4 high-priority bugs

**Decisions locked in:** Linear (team `PRO`, `GIT-` prefix, GraphQL API); file issues for **all** findings; implement only the **4 high-priority** fixes.

### 1. File Linear issues (8 total)
Resolve `PRO` teamId, search for dupes, then create via GraphQL `issueCreate`, all titled `GIT-...`:
- **4 high** (one each): #1 empty-DB stats crash, #2 `execute_query` param mismatch, #3 NULL `word_count` format, #4 malformed Markdown export
- **3 medium** (one each): #5 `importlib.reload` defeats caching, #6 test hooks leak into config, #7 hardcoded `2026`
- **1 grouped low**: #8 CREATE TABLE per call, #9 redundant query_params, #10 story markdown render, #11 bare excepts

`LINEAR_API_KEY`: I'll check `cortex secret list` and use inline injection; if it's not stored I'll ask you to add it via `/secrets`. The key is never printed.

### 2. Implement the 4 high-priority fixes
- **#1** `pages/archive_stats.py` — guard empty `df_years` (show `st.info`, return) before `.sum()`/metrics.
- **#2** `pages/favorites_tags.py` — replace the broken `execute_query(sql, tuple)` batch query with a direct `get_conn()` sqlite cursor (native `?` support). Leaves shared `downloader/db.py` untouched.
- **#3** `pages/search_explorer.py:53` + `pages/read_story.py:103` — coalesce `word_count` to `0` before `:,` formatting.
- **#4** `pages/read_story.py:85-94` — rebuild the export string without leading indentation so the `.md` is valid.
- Add regression tests for the empty-stats and favorites-year paths.

### Verification
- `uv run pytest tests/downloader/test_dashboard.py tests/downloader/test_dashboard_streamlit.py`
- `uv run ruff check src/storybuilder/dashboard scripts/dashboard.py`
- Manual smoke of the three fixed flows; confirm 8 `GIT-` issue URLs.

### Execution (team mode)
After approval I'll spin up the team: 1 worker for Linear filing (credentialed), 1–2 for the code fixes + tests, 1 verifier. Medium/low findings are tracked in Linear only, not coded now.

Full plan saved to `.cortex/plans/plan-dashboard-review.md`.